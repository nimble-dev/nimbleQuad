For now we are keeping the vignette outside the package and housing it on the NIMBLE website. This avoids any difficulties with formatting that might trigger CRAN complaints. 

Quadrature.yaml has the metadata we might use if we do make it a formal vignette, while Quadrature.qmd treats the vignette as a standard Quarto document. 
